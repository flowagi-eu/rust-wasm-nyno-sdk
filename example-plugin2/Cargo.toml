[package]
name = "example_plugin2"
version = "0.1.0"
edition = "2024"

[lib]
name = "plugin2"
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
plugin_sdk = { path = "../plugin_sdk" }
